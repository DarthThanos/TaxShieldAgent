export interface ConnectionSettings {
    host: string;
    port: number;
}
export declare let connectionSettings: ConnectionSettings;
export declare const setConnectionSettings: (settings: Partial<ConnectionSettings>) => void;
