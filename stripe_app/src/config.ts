/**
 * TaxShieldAgent Stripe App — Runtime Configuration
 *
 * BACKEND_URL is the only value you need to change between dev and production.
 *
 * Development:  set to "http://localhost:8001"  (not for submission)
 * Production:   "https://api.taxshieldagent.com" (required for Stripe review)
 */

export const BACKEND_URL = "https://api.taxshieldagent.com";
